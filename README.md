# BT Quote
Boomer T's shared pricing + quote engine.
One pricing brain for: Quick Quote page, employee portal, BT Catalog quote drawer, and the quote->design->WooCommerce pipeline.
Serves POST /wp-json/boomerts/v1/price and /quote. Auto-update: WordPress reads manifest.json via the GitHub API and installs the versioned zip.
